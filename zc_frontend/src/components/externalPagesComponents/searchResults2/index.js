import { StrictMode } from "react";
import ReactDOM from "react-dom";

import App from "./searchResults2.js";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <StrictMode>
    <App />
  </StrictMode>,
  rootElement
);
